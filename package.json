{
  "name": "cb-frontend",
  "version": "1.0.0",
  "main": "index.js",
  "homepage": "https://jvalamis.github.io/cb-frontend",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js",
    "dev": "nodemon --watch src --watch public --ext js,html,css src/index.js",
    "build": "mkdir -p dist && cp -r public/* dist/"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "passport": "^0.6.0",
    "passport-github2": "^0.1.12",
    "node-ssh": "^13.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}