// Create basic HTML elements
const app = document.createElement("div");
app.id = "app";
app.innerHTML = `
  <h1>Welcome to CB Front</h1>
  <p>This is a basic HTML setup</p>
`;

// Add the app div to the document body
document.body.appendChild(app);
